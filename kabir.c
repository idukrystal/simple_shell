#include "chris.h"
#include "main.h"
/**
 * kabir - excess func
 * @p: waste
 * @is_atty: atty
 * Return: nothing
 */
void kabir(int p, int is_atty)
{
	if (p == -1)
	{
		if (is_atty)
			_putchar('\n');
	}
}
